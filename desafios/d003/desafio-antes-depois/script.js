function clicou() {
  var num = Number(prompt("Digite um número inteiro qualquer!"));
  var antes = num - 1;
  var depois = num + 1;
  alert(
    `Antes de ${num}, temos o número ${antes}. Depois do ${num}, temos o número ${depois}`
  );
}
